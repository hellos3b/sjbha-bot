{
  "name": "@hellos3b/bored-bot",
  "version": "2.2.6",
  "private": true,
  "scripts": {
    "uptick": "tsx ./scripts/release_version.ts",
    "deploy": "docker compose -f docker-compose.yml build && docker compose -f docker-compose.yml up",
    "dev": "docker compose -f docker-compose.dev.yml up --build"
  },
  "devDependencies": {
    "semver": "^7.3.7",
    "tsx": "^3.9.0"
  }
}